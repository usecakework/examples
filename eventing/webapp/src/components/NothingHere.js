// show deploy from github button, don't wire it up
// show import {Navbar, Button, Alignment, Tab, Tabs} from "@blueprintjs/core"

function TestPanel() {
    return (
      <div className="nothinghere">
      Nothing here, this is just a mockup.
      </div>
    );
  }
  
export default TestPanel;
  